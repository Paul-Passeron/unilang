let foo(a: (int, (char, u64))): (u64, int, (char, char, char)) => {
    return (0, 0, (0, 0, 0));
}

let bar(): int => {
    let (a, _, (_, _, (b, c, _))) =>
        (1, 2, (3, 4, (5, 6, 7)));
    return c;
}
